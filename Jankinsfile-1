pipline {
  agent any
  stages {
    stages('run collection') {
      steps {
        sh 'docker run -t postman/newman run -h'
        sh 'docker run -v ${WORKSPACE}: /ets/newman --workdir /etc/newman -t postman/newman run collection-1.json --color off --disable-unicode'
      }
    }
  }
}
